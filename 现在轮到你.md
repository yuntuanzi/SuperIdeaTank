# 醒来先看这个

> Claude Opus 5 ｜ 2026-09-13 13:00 ｜ 距提交截止（9/15 10:00）约 45 小时

---

## 一、只有你能做的事（按紧急度）

### 1. ⏰ 立刻去活动页「创建项目占位」（5 分钟，越早越好）

官方开发者手册原文：

> 本次大赛特设人气奖，9 月 13 日项目广场将在知乎站内正式曝光。
> **不必等作品完全完成，哪怕只有一个 idea，也建议尽早提交占位，提前积累人气。**

- 人气奖统计窗口是 **9.13–9.23**（作品&想法的点赞量 + 使用量 + 评论量，2 支 × ¥2000）
- 越早占位曝光越久，**这是唯一一个「早做就白赚」的动作**
- 填最少信息即可，之后随时改：项目名「观点进化缸」、赛道「📚 知识炼金场」、
  一句话介绍用 `docs/产品说明计划书.md` 开头那句
- **创建后活动页会分配 OAuth `app_id` / `app_key`** —— 这是第 4 项的前提

### 2. 🚀 部署上线（约 5 分钟，这是唯一会让分数归零的一项）

初审「完成度」占 25%，而 Demo 打不开的话其余 75% 也无从谈起。

仓库已经推到 **`ok392637-netizen/opinion-evolution-tank`（现为 private）**，
根目录备好了 `render.yaml`。步骤：

1. <https://render.com> → GitHub 登录 → 授权这个仓库
2. New + → **Blueprint** → 选仓库（自动读 `render.yaml`）
3. Environment 里填 `ZHIHU_ACCESS_SECRET = 15761b…`（**只填控制台，别进仓库**）
4. Create，等 3–5 分钟

完整方案与备选（自有服务器 Docker / Zeabur）见 [`docs/DEPLOY.md`](docs/DEPLOY.md)。

> ⚠️ **不要用 Vercel / Netlify**。本项目靠进程内 + 磁盘缓存把「直答 2 次/日、
> 热榜 2 次/日」撑成评委可以随便点的 Demo，Serverless 每次冷启动丢缓存 = 额度当场烧光。
> 理由写在 DEPLOY.md 第 2 节。

### 3. 📖 把 GitHub 仓库改成 public

代码仓库是「评审重点加分项」，private 评委打不开。
我建的是 private，**改公开这一步留给你决定**。

### 4. 🤔 两个需要你拍板的决策

**A. 要不要接知乎 OAuth**（高分项）
官方明确「推荐接入 OAuth，登录人数会作为人气奖评定之一」，
且 `user_data` 额度 **1000/日**——是所有能力里唯一充裕的，评委可以随便玩不会耗尽。
可做「我的观点画像」：授权后读用户自己的创作/关注/收藏，
用同一套「物种 × 生存策略」模型分析**他本人**是哪种观点物种。
前置：第 1 项拿到 app_id/app_key + 第 2 项拿到公网回调地址。

**B. 那个旧的 Access Secret 要不要用**
`app/server/.env.bak.2026-09-13` 里还有一个更早的凭证（已 gitignore，没入库）。
它属于**另一个账号**，有独立额度池，我刚查是满额（含 2 次直答）。

**我没有动它，也没有把多凭证轮换做进产品。** 理由：赛事作弊条款写了
「同一设备、手机号、收货地址多账号参赛」判定违规。用第二个账号的 API 额度
是否越线，是**参赛资格问题**，该你判断，不该我替你决定。

### 5. 🎬 录演示视频（选交加分）

脚本已经写好：[`docs/演示视频脚本.md`](docs/演示视频脚本.md)
—— 3 分钟分镜 + 逐段旁白 + 录制前检查表 + 评委追问的备用答案。
录之前先打开一次线上页面唤醒（免费档会休眠）。

---

## 二、我做完了什么

### 你点名的两件事

**① 接入真实 API** —— 完成，但根因不在 API 上

新凭证已换好并跑通（`.env` 已切，旧的备份在 `.env.bak.2026-09-13`）。
但我发现真正的问题是**架构错配**，不是接没接上：

原代码把知乎**直答**当成「输出 JSON 的分类器」用。实测直答是**问答/检索产品**，
完全无视 system prompt 和「只输出 JSON」的要求，会把输入里的知乎问题当提问来答。
后果是标注全部落进降级分支——**40 个物种被清一色标成「中立·数据论证」，
生态缸就是一坨同色圆点，而系统毫无察觉。**

重构成双通道：

- **逐条标注** → 本地可解释模型（词典 + 句法 + 问题类型先验），零 API 额度，
  每个判定带命中证据，可以展开看「凭什么这么判」
- **整缸生态解说** → 直答，**自然语言提问不加格式约束**。这样用它质量极高：
  它自发分出 5 派、说清每派靠什么说服人、指出谁占上风，还标注了检索来源

标注质量（我逐条读原文人工标注了 22 条真实回答做锚点）：
第一批 12 条立场 10/12、策略 11/12；第二批 10 条立场从 **2/10 提到 10/10**。

**② 美化 UI** —— 完成第一轮，第二轮正在跑

第一轮已合并：物种节点从二维编码升到四维（面积=能量、颜色=立场、
圆内符号=生存策略、描边粗细=权威等级），加了入场补间动画，
观测窗改成仪器取景框，建立了四级字阶。截图在 `docs/ui-redesign/screenshots/`。

第二轮（已完成）：
- **修掉一个让生态缸看起来是死的真 bug**：能量归一化把两种量纲混在一起，
  90% 的节点被压到最小半径，看起来像「一颗恒星加九颗尘埃」
- **色轴按问题类型自适应**：「为什么…」型问题的回答本来就在解释不在站队，
  立场轴对它们不成立 —— 这类缸改用生存策略上色，并在缸顶说明原因
- **「未识别」如实呈现**：标注器判不出策略的回答不再被兜底成「数据论证」，
  改为不画符号 + 虚线描边 + 详情卡写「未识别」
- 物种详情卡新增「判定依据」（命中词 + 权重条）、低置信角标
- 右栏新增「AI 生态解说」区块，顺带填掉 1440 下那片空白
- 热榜「缸已就绪」徽标 + 已就绪条目排到最前（评委优先点这些，零额度秒开）
- 数据来源徽标改三态：实时 / 缓存·采集于X / 演示数据集

### 我自己加的

| 事项 | 为什么 |
|---|---|
| 读完官方赛规飞书文档 | 拿到**评分权重**：AI场景价值 40% / 创新度 25% / 完成度 25% / 设计感 10%。这重排了所有优先级 —— 部署 > 标注正确性 > UI |
| 抢在额度过期前囤了 9 个缸的原始素材 | 搜索和问题回答各 10 次/日**当天不用就作废**，而这是最贵的东西。90 条真实回答已落盘，标注可以离线重跑无限次 |
| 9 个演示缸预构建 + 永久持久化 | 评委点这些问题**零额度、秒开**。不然点两下额度就没了 |
| 热榜/搜索/问答缓存全部落盘 + 快照兜底 | Render 免费档 15 分钟休眠一次，纯内存缓存每次冷启动归零，而热榜只有 2 次/日 —— 这是会在评审当天出事的洞 |
| 写完产品说明计划书 | 必交材料、初审重点考核项 |
| 部署方案 + Dockerfile + render.yaml | 让部署变成 5 分钟的事 |
| 合规闸脚本 | `node tools/compliance-check.mjs --dist --url <线上域名>`，机械校验凭证泄漏/诚信标注/依赖变更，不靠人眼 |
| 39 条自动化测试 | 标注器、缓存、降级链、离线预构建全覆盖 |

### 所有改动前都做了备份

- 原始 zip：`~/dev/_backups/super-eco-ORIGINAL-2026-09-13.zip`
- 每次改源码前存 `<文件名>.bak*.2026-09-13`
- git 逐步提交，每条 commit message 都写清楚了「为什么」
- 凭证从未入库（脚本已验证，含旧凭证）

---

## 三、还没做完的

| 项 | 状态 |
|---|---|
| ~~前端第二轮~~ | ✅ 已完成并验收（见下） |
| 9 个预构建缸的 AI 生态解说 | ⏸ 今日直答额度 0，额度重置后补（每天只能补 2 个缸） |
| 5 个缸的「立场退化」告警 | 部分属实（归因型问题本来就没有立场轴），前端第二轮的色轴自适应会正面解决 |
| 部署 / 仓库公开 / 视频 / OAuth | ⏸ 等你 |

### 额度状态（今日已全部见底）

`zhihu_search 0` · `question_answers 0` · `zhida_openai 0` · `hot_list 1` · `user_data 1000`

观察到额度**不是本地午夜重置**——旧凭证在今天 10:26 到 12:05 之间整体归零重置了。
所以大概率是每天固定某个时刻（约 11:00–12:00）或滚动窗口。
**明天重置后第一件事：给预构建缸补 AI 生态解说**（`scripts/prebuild-tanks.mjs` 在线模式）。

---

## 四、一句话总结

产品从「跑得起来但核心概念没被呈现」推到了「概念真的落地、且能扛住评审当天的额度约束」。
**剩下卡住的只有部署那一步，而那一步需要你登录。**
