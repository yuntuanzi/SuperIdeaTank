# 部署说明（给负责域名/部署的队友）

> 项目：观点进化缸 · Opinion Evolution Tank ｜ 知乎黑客松 2026 校园新锐季
> 仓库：<https://github.com/ok392637-netizen/opinion-evolution-tank>
> **赛事截止：2026-09-15 10:00，截止后不接受任何补交。**

---

## 0. 你需要做的，一共三步

1. 部署（拿到公网 HTTPS 域名）
2. 把回调地址登记到赛事页面
3. 在部署平台填 4 个环境变量

做完这三步，OAuth 登录和「我的观点画像」就活了。**代码不用改一行。**

---

## 1. 部署

### 推荐：Render.com 免费档

仓库根目录已有 `render.yaml`，不用手动配置。

1. <https://render.com> → GitHub 登录 → 授权 `opinion-evolution-tank` 仓库
2. Dashboard → **New +** → **Blueprint** → 选这个仓库（自动读 `render.yaml`）
3. 先别急着 Create，往下看第 3 节把环境变量填了
4. Create，等 3–5 分钟

### ⚠️ 千万别用 Vercel / Netlify / Cloudflare Workers

不是偏好问题，是**会当场毁掉 Demo**：

本项目每天可用的接口额度极少（实测：直答 **2 次**、热榜 **2 次**、
知乎搜索 **10 次**、问题回答 **10 次**）。产品靠**进程内缓存 + 磁盘缓存**
把这点额度撑成评委可以随便点的 Demo。

Serverless 每次冷启动都是全新实例、无共享内存、无可写磁盘 →
缓存全部失效 → **评委刷两下首页，当天额度就没了**。

必须部署成一个**长驻的 Node 进程**。Render / Zeabur / 自有服务器 Docker 都行。

### 如果你有自己的服务器（比 Render 免费档更好）

不休眠、国内延迟低。仓库根有 `Dockerfile`：

```bash
git clone https://github.com/ok392637-netizen/opinion-evolution-tank.git
cd opinion-evolution-tank
docker build -t opinion-tank .
docker run -d --name opinion-tank \
  -p 8787:8787 \
  -e ZHIHU_ACCESS_SECRET='<向罗其立要>' \
  -e ZHIHU_OAUTH_APP_ID='531' \
  -e ZHIHU_OAUTH_APP_KEY='<向罗其立要>' \
  -e ZHIHU_OAUTH_REDIRECT_URI='https://<你的域名>/auth/callback' \
  -v /opt/opinion-tank-data:/app/server/data \
  --restart unless-stopped \
  opinion-tank
```

挂了 `-v` 持久卷之后，生态缸缓存跨重启不丢，比 Render 免费档更稳。
再用 Nginx 反代 + certbot 上 HTTPS。

**HTTPS 是硬要求**：知乎 OAuth 回调不接受 http，评委也多半从微信/知乎内嵌浏览器打开。

---

## 2. 登记回调地址

到赛事活动页（<https://www.zhihu.com/hackathon?activity_code=zhihu_hackathon_2026_p2>）
→ 项目设置 → 知乎登录回调地址，填：

```
https://<你的域名>/auth/callback
```

> ⚠️ **必须与第 3 步配置的 `ZHIHU_OAUTH_REDIRECT_URI` 完全一致**——
> 协议、域名、路径、**尾部斜杠**，一个字符都不能差。
> 不一致的表现是：跳转回来后报 `CODE_MISSING`，很难猜。

---

## 3. 环境变量（4 个）

在 Render 的 **Environment** 面板（或 Docker 的 `-e`）里填：

| 变量名 | 值 | 说明 |
|---|---|---|
| `ZHIHU_ACCESS_SECRET` | **向罗其立索取** | 开放平台调用方凭证 |
| `ZHIHU_OAUTH_APP_ID` | `531` | 公开标识，可以明文 |
| `ZHIHU_OAUTH_APP_KEY` | **向罗其立索取** | 保密，只填平台 Secret |
| `ZHIHU_OAUTH_REDIRECT_URI` | `https://<你的域名>/auth/callback` | 与第 2 步登记值完全一致 |

### 🔒 两个密钥的传递方式

**不要通过微信/QQ/邮件明文发，也不要截图。**
建议：罗其立本人直接在部署平台的控制台里输入，或用一次性密文工具（如
<https://privatebin.net> 设阅后即焚）传给你。

**绝不能提交进 Git 仓库**——这是赛事红线，会取消资格。
仓库的 `.gitignore` 已经覆盖了所有 `.env*` 文件，正常操作不会误传。

---

## 4. 验收（照着走，每步都有可观察的结果）

```bash
# ① 服务活着，且接上了真实凭证
curl https://<你的域名>/api/status
# 期望：{"liveMode":true, ...}   liveMode 为 false 说明 ACCESS_SECRET 没生效

# ② OAuth 配置齐了
curl https://<你的域名>/api/oauth/status
# 期望：{"configured":true,"callbackConfigured":true,"authorized":false,"appId":"531",...}
# configured 为 false → 三个 OAuth 变量有缺
```

浏览器里：

3. 打开首页 → 顶栏「授权知乎账号」按钮**从灰变成可点**
   （没变说明 `ZHIHU_OAUTH_REDIRECT_URI` 没配上）
4. 点它 → 跳知乎授权页 → **本人亲自点确认**（不能代点，赛事要求）
   → 跳回 `/?oauth=ok`，顶栏显示昵称/头像
5. 点「我的观点画像」→ 看到你自己的立场/策略分布 + 样本 + 判定依据
   （全新小号看到「你的公开创作还不够做画像」是**正常空态**，不是 bug）
6. 连点两次画像：第二次应命中 30 分钟会话缓存，不再消耗额度
7. 走一遍主流程：热榜 → 点任意标「**缸已就绪**」的问题 → 生态缸 →
   时间轴 → 环境干预 → 放生实验 → 导出报告

### 出问题时看这里

| 现象 | 多半是 |
|---|---|
| 按钮一直灰 | `ZHIHU_OAUTH_REDIRECT_URI` 没配或为空 |
| 跳回来 `?oauth=err`，`error.code = CODE_MISSING` | 回调地址与活动页登记值不一致（查尾斜杠） |
| `error.code = STATE_MISMATCH` | 授权中途换了浏览器，或会话 cookie 丢了，重来一次 |
| `stateVerified: false` | 知乎实测回调可能不带 `state`，**这是已知协议缺口不是 bug** |
| 首页热榜是演示数据 | 当天热榜额度（2 次）已耗尽，会自动降级到提交进仓库的快照，界面有如实标注 |

---

## 5. 两件跟部署有关、但容易踩的事

### 5.1 Render 免费档会休眠

15 分钟无请求就休眠，下次访问冷启动 **30–50 秒**。

**评审期（9/15–9/17 评委滚动打分）这是真风险。**
把链接交出去之前**先自己打开一次唤醒**；或者花 7 美元/月升 Starter 档，永不休眠。
这是最省心的一笔钱。

### 5.2 演示缸是赛前预构建的资产，别删

`app/server/data/prebuilt-tanks.json` 里是 **9 个已经构建好的生态缸**
（90 条真实知乎回答），是提前花掉接口额度换来的。
评委点热榜里标「缸已就绪」的问题时**零额度、秒开**。

这个文件**随仓库提交**，不要当成缓存清掉。

---

## 6. 项目本身要了解的

只读三份就够：

| 文档 | 内容 |
|---|---|
| [`README.md`](README.md) | 产品是什么 + 两个核心工程决策 |
| [`docs/产品说明计划书.md`](docs/产品说明计划书.md) | **赛事必交材料**，评委初审看的就是这份 |
| [`docs/DEPLOY.md`](docs/DEPLOY.md) | 部署方案的完整论证（含为什么不能用 Serverless） |

本地跑起来（不填任何凭证，走演示模式）：

```bash
cd app/server && npm install
cd ../web && npm install && npm run build
cd ../server && npm start     # http://localhost:8787
```

---

## 7. 部署完成后请回传

1. **线上域名** —— 要填进产品说明计划书和活动页
2. `/api/status` 和 `/api/oauth/status` 的返回

拿到域名后还剩两件事：录 3 分钟演示视频（脚本已写好在
[`docs/演示视频脚本.md`](docs/演示视频脚本.md)），以及在活动页提交作品。
